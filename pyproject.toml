[project]
name = "signature"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bcrypt>=5.0.0",
    "cryptography>=48.0.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
